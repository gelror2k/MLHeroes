<?php
/**
 * Database configuration template.
 *
 *   1. Copy this file to  config/database.php   (same folder)
 *   2. Fill in the values from Freehostia  ->  Control Panel  ->  MySQL Databases
 *   3. Never commit database.php — it is listed in .gitignore
 *
 * On Freehostia the MySQL user is usually the same as the database name,
 * and the host is shown next to the database in the control panel
 * (it is often NOT "localhost" — copy exactly what the panel shows).
 */

define('DB_HOST', 'localhost');
define('DB_NAME', 'gabcas7_gelodb');
define('DB_USER', 'gabcas7_gelodb');
define('DB_PASS', 'gelodatabasefreehostia123?');
define('DB_CHARSET', 'utf8mb4');
